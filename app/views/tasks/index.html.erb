<ul>
  <% @tasks.each do |t| %>
    <li><i class="fa-regular fa-circle<%= t.completed ? "-check" : ''%>"></i> <%= link_to t.title, task_path(t) %></li>
  <% end %>
</ul>
<%= link_to "+ New task", new_task_path, :class => 'btn btn-primary m-2' %>
